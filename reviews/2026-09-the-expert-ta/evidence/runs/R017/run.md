# Test Run R017 — S3

| | |
|---|---|
| **Run ID** | R017 |
| **Date/time** | 2026-09-10 12:50 |
| **View / sample** | S3 |
| **Page URL / location** | UI: Class Management (Accessibility Mode) → row Actions select → Take Assignment → Go |
| **Task / process** | T1 |
| **Modality** | no-vision |
| **Tool** | nvda |
| **Baseline** | B5 |
| **Tester** | Daniel Fontaine (reviewer, NVDA 2026.2, B5); assistant records |
| **Result** | **Broken** — reviewer's decision 2026-09-10: "if they can't complete the entire problem set because a single problem is inaccessible then the whole problem set is not accessible." Blocker-level: multiple-choice parts whose options contain math (V-F11 + V-F9). Completable with barriers: text-only multiple choice, symbolic/numeric entry (V-F13), drag-and-drop ranking (V-F14), free-body diagram (pass), hints (V-F15), submission dialogs (pass). |

## Checks (no-vision)

Outcome: pass / fail / partial / n/a — every row must get one before the
run's Result is set. Fails cite observation IDs.

| Check | Outcome | Observations |
|-------|---------|--------------|
| NV1 — Page/view title identifies its purpose | pass | page change announced "Take Homework Assignment" on arrival (R016 O13) |
| NV2 — Headings and landmarks exist, are hierarchical, and support navigation | fail | O1 — no headings; after activating a problem link there is no structural way to reach the problem region |
| NV3 — Every control announces an accurate name, role, and value/state | fail | O2 (jump point unnamed), O6/O9 (answer radios: no name; focus mode reads the column-two text but math in options does not reliably play; mixed text+math auto-read breaks) |
| NV4 — Images announce appropriate alternatives; decorative images are silent | fail | O8 — the Problem 8 figure is not exposed: `G` finds no graphic, the image cannot be right-clicked; the diagram is hidden from the screen reader. Math (O7) is readable in browse mode — not an NV4 failure |
| NV5 — Reading order matches the meaning of the visual order | pass | no order problem reported while arrowing through statement → parts → answer area → Submit; the answer area's boundaries are simply unmarked (V-F13) |
| NV6 — Form fields announce labels and instructions; errors are announced and identified | fail | O6/O9 (radios unlabeled), O15 (symbolic answer field: nothing identifies it as F_NET — the visible MathJax prefix is not associated) |
| NV7 — Dynamic updates (toasts, async results, validation) are announced without stealing focus | fail | O3 — Ctrl+Shift+5 works; O4 — Ctrl+Shift+2 reads raw MathJax markup for math answers (V-F9); O10/O14 — submission dialogs announced correctly; O19 — drag-and-drop placements not announced (V-F14); O23 — inserted hint not announced (V-F15) |
| NV8 — Nothing is conveyed only by visual position, shape, or size | fail | O6/O9 — a math-bearing answer option is identified to the screen reader only by its table position ("row 4 table 1"); O23 — the inserted hint is findable only by its position below the problem |
| NV9 — Language of the view (and passages) is announced/pronounced from the correct language | partial | page has no `lang` attribute (R004 O2 — technical 3.1.1 failure); no mispronunciation reported with an English synthesizer voice |

## Observations

Raw narration captured during the session. One numbered entry per thing
noticed; lifecycle: new → clarified → classified → finding:<ID> or dismissed.

Format:
- O1 [new] (state: which view state): what happened
  - Classified: <check ID> / WCAG <SC> / <severity> → finding <ID> | dismissed: <reason>


- O1 [clarified] (state: Take Assignment, Problem 9 active on arrival): `K` moves through all nine problem links ("Problem N Click To Activate"). Pressing Enter on one activates the problem but **focus is not placed into the problem region**; with no headings on the page the reviewer found it unclear how to get to the newly loaded problem.
  - Classified: NV2, NV5 / WCAG 2.4.3 (focus not managed to the activated content), 1.3.1 (no headings to locate it) / **Major** → finding T1-F2 (task T1 step 4)
- O2 [clarified] (state: as O1): tabbing reaches a **visually hidden** div announcing "Press tab to go to problems. Press enter to open the accessibility shortcuts menu.  button" — the problems jump point. Enter **mounts new links** (the accessibility shortcuts menu) that move around the page. A keyboard user can Tab to them; under NVDA the reviewer had to switch to browse mode to reach them. Reviewer's assessment: "very confusing for a screen reader user" — the pseudo skip links are only discoverable by first tabbing into a hidden area.
  - Classified: NV3 / WCAG 4.1.2 (control has no name — only its instruction text), 2.4.3 (mounted links not in the natural focus path) / **Major** → finding V-F8 re-rated (was Minor on Class Management); **gap question for the keyboard run:** is anything visible on screen when the hidden jump point has focus (2.4.7)?
  - Vendor claim status: the "context-specific menus while tabbing" exist on this page; their discoverability and naming are the defects.
- O3 [clarified] (state: Problem 9, multiple choice, focus in the answer area): `Ctrl+Shift+5` moves NVDA to the alert region and reads: "alert  Multiple Choice. Press Ctrl+Shift+2 keys to read out your answer. Supplemental answer not available. Ctrl+Shift+4 keys for details. Ctrl+Shift+5 keys for instructions.". The chord reached the page under NVDA and the live region was spoken.
  - Classified: NV7 / pass for this chord (4.1.3 works as designed for instructions)
- O4 [clarified] (state: as O3, after selecting an option): `Ctrl+Shift+2` ("read out your answer") writes the selected option's **raw MathJax HTML source** into the alert region. Speech Viewer begins "alert  <span class="MathJax_Preview" style="color: inherit display: none"><span class="MathJax" id="MathJax minus Element minus 78 minus Frame" tabindex="0" data minus mathml="ltmath xmlns=quothttp: divided by divided by www.w3.org …" and continues for ~4,400 characters of span/style/attribute markup (hyphens read as "minus", slashes as "divided by") before any answer text; the actual answer (T₂ is less than T₁, rendered with MathJax) is buried inside as `<script type="math/tex">T_2</script> is less than …`. Evidence: `R017-ctrlshift2-speech.txt` (reviewer's Speech Viewer capture).
  - Classified: NV7 / WCAG 4.1.3 (status message content is not usable), 1.3.1 / **Blocker for the read-back function, Major for the task** → finding V-F9. Refutes the vendor's "human-friendly spoken math" claim for the answer read-back on multiple-choice parts with math in the options; any option containing MathJax will read this way.
  - Answered 2026-09-10: (a) `Ctrl+Shift+4` (details) works fine — clean text. (b) On a plain-text option `Ctrl+Shift+2` reads the words correctly; on a math option ("½ g") it still reads the raw MathJax markup. So V-F9 is specific to math-bearing content.
- O5 [clarified] (state: Problem 9 / Problem 8): `Ctrl+Shift+4` (details) produces clean text — works.
  - Classified: NV7 / pass for this chord
- O6 [clarified] (state: Problem 8 and 9 answer options, arrowing between the radios in browse mode): plain-text options **read correctly** as the reviewer arrows between them — NVDA reads the adjacent cell text even though the radios have no label (R004 O1). Math options do not: "½ g" announces as **"row 4 table 1"** — position only, no content — and `Ctrl+Shift+2` on it reads the raw MathJax markup (V-F9).
  - Classified: NV3, NV6 / WCAG 1.3.1, 4.1.2 / **Major** (Blocker for any part whose options contain math — common in physics) → finding V-F11. Narrows R004 O1: the unnamed radios are mitigated for text options by NVDA's browse-mode cell reading (a focus-mode Tab would not read them — to confirm), not for math.
- O7 [clarified] (state: Problem 8 statement): the MathJax expressions **M₁ and M₂ announce as "table"** — the mathematical content of the problem statement is not read to the screen-reader user at all.
  - **Revised 2026-09-10:** the math **is readable in NVDA browse mode** — moving the browse cursor into the MathJax item reads it; "table" was the focus-mode reading. So the statement math is programmatically available and this is not a 1.3.1/1.1.1 failure; the cost is that the user must know to leave focus mode (which the answer widgets keep them in). V-F10 **withdrawn as a failure**, kept as an advisory note; the math-in-options problem stays in V-F11. `Ctrl+Shift+2` reads raw markup in both modes (V-F9 stands). Instrument check no longer needed.
- O8 [clarified] (state: Problem 8 statement): `G` (next graphic) finds nothing — the two-blocks-and-pulley figure is not exposed as a graphic; the reviewer also cannot right-click it. Exploration found the Problem 6–9 figure `<img>` with **empty alt** (decorative to AT); NVDA therefore skips it. The figure carries the physical setup the statement refers to ("as shown").
  - Classified: NV4 / WCAG 1.1.1 / **Major** → finding V-F12
- O9 [clarified] (state: Problem 8 answer group, entered with Shift+Tab from Submit — **focus mode**): entering announces "table"; in the hidden column 1 the reviewer can arrow between the radios; moving focus auto-selects (standard radio-group behaviour) and announces "Radio Button Checked X of X"; the column-two answer text is heard, but MathJax inside an option does not reliably play — the reviewer must leave focus mode and move the browse cursor into the MathJax item to hear it. "The table's attempt to auto-read the answer breaks because it won't correctly read the inserted MathJax and the plain text combined."
  - Classified: NV3, NV6 / WCAG 1.3.1, 4.1.2 / Major (Blocker for math options) → V-F11 updated (focus-mode behaviour now known)
- O10 [clarified] (state: Problem 9, option selected → **Submit**; Task T2 steps 1–3): Submit opens a small dialog that voices correct/incorrect with further options; "it works as expected." Focus moves into the dialog, onto a `div` close control announced as "click to activate" (a link-like close). One Problem 9 attempt consumed.
  - Classified: NV7 / pass for submission feedback (4.1.3 met here; the V-F9 failure is specific to the Ctrl+Shift+2 read-back). Answered (O11): the dialog is announced with role dialog and name "Submission Details"; the close control is "Close and stay on this question - Click To Activate" — adequately named. No finding.
- O11 [clarified] (state: Problem 9, a second Submit with the same option): Speech Viewer: "Submission Details  dialog  Incorrect Answer You have previously used this answer. Please refer to your submission history. Close and stay on this question - Click To Activate.". Role dialog + name announced; the message identifies the error and points to the submission history. (Second P9 attempt consumed.)
  - Classified: NV7, NV6 (error identification) / pass; resolves O10's naming question. Advisory pattern: every control name ends in "- Click To Activate" (from `title` text) — verbose but not a failure.
- O12 [clarified] (state: Problem 2, Part (a) "F_NET =" symbolic field, typed `m*a`): Tab from the field goes **directly to Submit** — the ~45-button symbol palette is **not in the tab order**. Field announcement: see O15 — nothing identifies the field as F_NET.
  - Classified: MO5 / pass for focus order (the palette does not burden tabbing). **Open question:** can a keyboard-only user enter palette-only symbols (θ, β, √, superscripts) without the mouse — typed aliases (`theta`, `sqrt()`, `^`)? If not, the palette being unreachable is a 2.1.1 issue for those parts (W20).
- O13 [clarified] (state: Problem 2, multi-part): `Ctrl+Shift+1` moves focus to the **first hidden accessibility guide** (the top jump point), not to the previous part as the vendor's script intends.
  - Classified: NV3 / advisory — folded into V-F8's description of the jump-point mechanism (documented chord does not do what its instruction says); no separate finding.
- O14 [clarified] (state: Problem 2 (a) → Submit): Speech Viewer: "Submission Details  dialog  Correct Answer Continue to the next question Click To Activate Close and stay on this question - Click To Activate", then focus on "Continue to the next question Click To Activate, visited link". Correct answer accepted; dialog announced; focus moves into the dialog.
  - Classified: NV7 / pass. (One P2(a) attempt consumed.)
- O15 [clarified] (state: Problem 2, Part (a) answer field): the reviewer "doesn't see anything labeled as F_NET" — on reaching the field nothing announces which quantity it asks for. The visible "F_NET =" prefix is a MathJax expression beside the field, not an associated label (exploration: the field is a custom editor, which is also why axe raised no `label` violation on it).
  - Classified: NV6 / WCAG 1.3.1, 3.3.2, 4.1.2 / **Major** (multi-part problems have several such fields per page; a screen-reader user cannot tell which part/quantity a field belongs to) → finding V-F13. **Open:** the exact announcement on focus (e.g. "edit", "edit multi line", anything else?).
- O16 [clarified] (state: Problem 2 Part (a)): the reviewer now identifies the "F_NET =" row as the **calculator** — the answer field plus the on-screen symbol/number palette — and calls it "a whole different area that we need to test". `Ctrl+Shift+2` reads what is in the F_NET field, but the reviewer is "not sure how to naturally navigate to it": there is no obvious path (Tab order, heading, or label) that brings a screen-reader user to the answer field of a part.
  - Classified: NV3, NV6 / strengthens **V-F13** (the field is neither labelled nor discoverable); the calculator widget itself is scheduled as its own step (walkthrough W15b: reaching it, button names, insertion into the field, read-back with Ctrl+Shift+3). **Open:** how did you reach the field the first time (mouse click? Tab from the part jump point?)
- O17 [clarified] (state: Problem 2 Part (a)): the reviewer can reach the answer area with the arrow keys in browse mode, but **nothing marks the entry into it** — "we just hear what's in it": "F_NET = [math stuff]", and the math notation still reads as "Table" there. So the field is reachable by linear reading, not by any control semantics, and the prefix math is not spoken in that flow.
  - Classified: NV3, NV6 / folded into **V-F13** (no label, no boundary announcement); math-as-"Table" in the answer row → V-F11/V-F10 advisory family
- O18 [new — assistant DOM reading, 2026-09-10] (state: Problem 3, the accessibility icon at the top-right of the part activated — screenshot `R017-p3-dnd-accessible-form.png`): the drag-and-drop part has an **alternative form** that opens beside the drop zone: a table `#dndGrid` (header row Bucket / Order / Item / delete; no caption, no ARIA) with one row per placed item; each row holds two **custom ARIA combo boxes** (`div role="combobox" tabindex="0" aria-haspopup="listbox" aria-expanded`, `aria-label="Selected item = '…'. Press Space or Enter to select an item"`) opening a `role=listbox` of `role=option` divs — the Bucket picker ("-- choose --", "Bucket 1") and the Item picker whose options are **text descriptions of the cards** (e.g. "A block labeled with mass "1 divided by 2 m" is placed on a smooth horizontal surface. An arrow pointing to the right indicates a force labeled "2F" being applied to the block."); an "Add Item" button (`aria-label` explaining it adds a row and moves focus to an unset selector) and a "Reset" button; a ✖ delete control per row. The card images in the visual area carry empty alt — the descriptions exist only in the form. Live regions `#calc-announce` / `#calc-live` were empty at the time of reading. One item (½m, 2F) was already placed in Bucket 1 / order 1; Problem 3 shows 3 attempts remaining.
  - Recon for W17 (to be verified by the reviewer, not a finding): do the custom combo boxes announce role, name, expanded state and the chosen option under NVDA; is a placement announced; is the form itself discoverable (the toggle is an `<img alt="Show/Hide Accessibility Statement">` inside a clickable element); does the ✖ have a name (AX tree shows 4 unnamed buttons on this state).
- O19 [clarified] (state: Problem 3, drag-and-drop alternative form, NVDA): (1) the toggle is an exposed **button**, reachable by Tab, announced "Show drag and drop accessibility table button"; activating it **moves focus** into the form. (2) To hear the current answer the reviewer must use `Ctrl+Shift+2`; the read-back reads **only the Item column** and runs the rows together "like a long paragraph" with no announcement of where a new row/item begins — "not unusable", but the user cannot tell which item is which. (4) A placement or the ranked result is **not announced in any designed way**; it can be derived by navigating the table.
  - Classified: NV7 / WCAG 4.1.3 (status of the ranking not conveyed; read-back lacks item boundaries) / **Minor** → finding V-F14. The vendor's "ranking drag-and-drop is fully compliant" claim largely holds on the mechanism (an operable, focus-managed alternative form exists) with these read-back gaps. Answered: the combo boxes are properly announced (role, name, options, choice); the ✖ delete button is properly announced; the visual cards outside the form announce **nothing** (empty alt — the descriptions exist only in the form, which serves as the accessible alternative, so not a separate 1.1.1 failure); Submit opens the same "Submission Details" dialog announcing correct/incorrect (one Problem 3 attempt consumed). W17 complete.
- O20 [clarified] (state: Problem 1 Part (a), free-body diagram tool, keyboard only + NVDA): Add Force, setting the force's angle and length through the force table, the `Ctrl+Shift+3` force-totals read-back, and Submit were **all successful without a mouse**; the reviewer calls the read-back feature "quite robust". (One Problem 1(a) attempt consumed.)
  - Classified: NV3, NV6, MO1, MO2, MO7 / **pass** — the vendor's claim that the FBD drawing question is accessible is confirmed for keyboard + NVDA on this problem. No finding. (axe's `empty-table-header` incomplete on the force table, R004 O11 → dismissed: headers announced in use.)
- O21 [clarified] (state: Problem 5, numeric part with the function keypad — the "calculator"): all keypad buttons are exposed, named and **Tab-reachable**, but only by linear Tab — no arrow-key movement within the keypad. (Contrast O12: on Problem 2's symbol palette Tab skipped straight to Submit.) The on-screen ←, →, HOME and END buttons move the entry cursor. The unit radio buttons **function as expected** (named).
  - Classified: MO1, MO2 / pass for reachability; advisory on tab-order burden (≈45 stops per part, no arrow navigation) and on the inconsistency between problem types; not a WCAG failure by itself.
- O22 [clarified] (state: Problem 5, the formula entry area): the reviewer finds **no way to reach the entry field directly** — only by tabbing to the nearest element and arrowing in (browse mode). The entry area is not a form control at all: reviewer-pasted markup `<div class="problemanswer" id="problem_answer">332&nbsp;fact(&nbsp;exp(<span style="color: red; text-decoration: blink">|</span>&nbsp;)&nbsp;)</div>` — a `div` holding the typed expression and a blinking-caret span. Its content is heard only via `Ctrl+Shift+2`: empty → "Alert your answer in degrees" (units context), populated → read left to right, "not PEMDAS" (no nesting/structure conveyed). The caret can be moved with the on-screen buttons, but there is **no feedback on where the caret is** within the formula while authoring.
  - Classified: NV3, NV6, MO1 / WCAG 4.1.2 (the entry area has no role, name, value or focusable state), 1.3.1, 2.4.3 (not in the focus order; unreachable directly) / **Major** → folded into **V-F13**, which now covers the whole entry area, not just its label. Answered: **`Ctrl+Shift+3` does report where the caret is** — so caret position is available on demand through the vendor's chord, not through the control itself; V-F13's "no caret feedback" narrows to "no feedback while moving the caret; available on request via Ctrl+Shift+3".
- O23 [clarified] (state: a problem with hints available, **Hint** button activated): the hint text appears below the problem area but **is not announced** when the button is pressed, and the reviewer found no clear way to navigate into it. Speech Viewer at the moment of activation (`R017-hint-speech.txt`): "table  with 3 rows and 1 column  Submissions Info." — NVDA reported only the Submissions Info table, not the hint. The deduction percentages (hints / feedback) **are** spoken.
  - Classified: NV7 / WCAG 4.1.3 (content added in response to the user's action with no status message), 2.4.3 (focus not managed to the new content) / **Major** (a hint costs a deduction; the screen-reader user pays it and hears nothing) → finding V-F15
- O24 [clarified] (state: **I give up!** activated): a modal opens with a warning and Continue / Cancel links — confirmation before forfeiting exists.
  - Classified: NV7, CO4 / pass (3.3.4-style confirmation present; no finding)

## Notes

(JAWS: Action / Announced / Expected-vs-actual triplets — copy exact speech
from Speech History, Insert+Space then H. WAVE: summary counts — Errors,
Contrast Errors, Alerts, Features/Structural — plus each distinct error type.
Conventions: ontology/testing-tools.md.)

2026-09-10 — Reviewer narration (NVDA 2026.2). Speech Viewer excerpt for O2: "Press tab to go to problems. Press enter to open the accessibility shortcuts menu.  button". Arrived via Accessibility Mode (S2) Actions select; problem 9 was active.

## Evidence files in this folder

- `R017-ctrlshift2-speech.txt` — reviewer's NVDA Speech Viewer capture of the Ctrl+Shift+5 instruction and the Ctrl+Shift+2 answer read-back (raw MathJax markup), copied from `evidence/mathjax.txt`
- `R017-p3-dnd-accessible-form.png` — Problem 3 with the drag-and-drop alternative form open (assistant screenshot over CDP during the reviewer's session)
- `R017-hint-speech.txt` — reviewer's Speech Viewer capture on pressing Hint (only "table with 3 rows and 1 column Submissions Info." spoken), copied from `evidence/hint.txt`

## Findings raised from this run

- T1-F2 (04 §A); V-F8 re-rated, V-F9, V-F11, V-F12, V-F13, V-F14, V-F15 (04 §B); V-F10 withdrawn (advisory)
